{
	"version": 2,
	"installCommand": "npm i  --legacy-peer-deps ",
	"buildCommand": "npx prisma db push && npx prisma db seed && npm run vercel-build",
	"builds": [
		{
			"config": {
				"installCommand": "npm i --legacy-peer-deps --force"
			},
			"src": "src/main.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/main.ts",
			"methods": [
				"GET",
				"POST",
				"PUT",
				"DELETE"
			]
		}
	]
}